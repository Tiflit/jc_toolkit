// JoyConProtocol.cpp
//
// All protocol types are defined in JoyConProtocol.h.
// Per-device mutable state (timing byte, connection status, etc.) is owned
// by JoyConDevice, not kept as globals here.
//
// This translation unit is intentionally empty. It exists so the build
// system has a .cpp to compile for the libjctool target.
